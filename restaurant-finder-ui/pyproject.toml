[project]
name = "restaurant-finder-ui"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "chainlit>=1.0.0",
    "boto3>=1.35.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"